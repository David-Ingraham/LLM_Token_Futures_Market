/** Index and USDC amounts use 6 decimal places (matches SPL USDC). */
export const MICRO_PER_UNIT = 1_000_000;

/** v1: one contract = one million output tokens of index exposure */
export const DEFAULT_CONTRACT_MTOK = 1;
